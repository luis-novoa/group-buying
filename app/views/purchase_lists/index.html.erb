<section class='round-box fit-content'>
  <article class='pending-deliveries'>

  <h2>Compradores com Entregas Pendentes</h2>

  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, purchase_list_path(user) %></li>
    <% end %>
  </ul>
  </article>
</section>